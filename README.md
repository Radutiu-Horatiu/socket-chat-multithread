# socket-chat-multithread
Simple socket example in python which uses multithreading to build a chat.

1. Open the project in your text editor(example: Visual Code)
2. Open 2 terminals
3. python server.py
4. python client.py
